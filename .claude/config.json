{
  $schema: https://json.schemastore.org/mcp-settings.json,
  mcpServers: {
    filesystem: {
      command: npx,
      args: [-y, @modelcontextprotocol/server-filesystem, D:\\social-platform-workspace]
    },
    memory: {
      command: npx,
      args: [-y, @modelcontextprotocol/server-memory]
    }
  }
}
